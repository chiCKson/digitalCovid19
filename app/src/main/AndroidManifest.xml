<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.company_name.digital_covid19">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label,android:theme">
        <activity
            android:name=".activity.StatisticActivity"
            android:theme="@style/SymptomActivityTheme" />
        <activity
            android:name=".activity.CountrySelectionActivity"
            android:theme="@style/SymptomActivityTheme" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activity.MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/HomeActivityTheme" />

        <uses-library android:name="com.google.android.maps" />

        <activity
            android:name=".activity.WelcomeActivity"
            android:label="@string/app_name"
            android:theme="@style/WelcomeActivityTheme">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.AddLocationActivity"
            android:label=""
            android:theme="@style/AddLocationActivityTheme" />
        <activity
            android:name=".activity.ActivityModelActivity"
            android:label=""
            android:theme="@style/ActivityModelActivityTheme" />
        <activity
            android:name=".activity.SignInActivity"
            android:label=""
            android:theme="@style/SignInActivityTheme" />
        <activity
            android:name=".activity.HomeActivity"
            android:label=""
            android:theme="@style/HomeActivityTheme" />
        <activity
            android:name=".activity.SymptomActivity"
            android:label=""
            android:theme="@style/SymptomActivityTheme" />
        <activity
            android:name=".activity.AddEventModalActivity"
            android:label=""
            android:theme="@style/AddEventModalActivityTheme" />
        <activity
            android:name=".activity.AddTransportModalActivity"
            android:label=""
            android:theme="@style/AddTransportModalActivityTheme" />
        <activity
            android:name=".activity.SymptomsSecondActivity"
            android:label=""
            android:theme="@style/SymptomsSecondActivityTheme" />
        <activity
            android:name=".activity.RegisterActivity"
            android:label=""
            android:theme="@style/RegisterActivityTheme" />
    </application>

</manifest>